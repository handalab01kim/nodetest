// const http = require('http');

// [4001, 5000, 5001, 4010].forEach(port => {
//   const server = http.createServer((req, res) => {
//     console.log(`[${port}] ${req.method} ${req.url}`);
//     res.end('OK');
//   });
//   server.listen(port, () => console.log(`Listening on ${port}`));
// });
